---
name: 스킬테스터-스모크
description: API 기반 스모크 테스트 실행 및 자동 디버깅 (Health Check, JWT, 주요 API, DB 연결)
---

# 스킬테스터-스모크 (Smoke Test)

## 개요
YAML 체크리스트 기반으로 프로젝트의 필수 기능들을 1분 이내에 빠르게 검증하고,
실패 시 원인을 분석하여 해결책을 제안하는 스모크 테스트를 실행합니다.

## 특징
| 항목 | 값 |
|------|-----|
| 브라우저 | ❌ 불필요 (API 테스트 위주) |
| Google 로그인 | ❌ 불필요 (Health Check 수준) |
| 스크린샷 | ❌ 없음 |
| 속도 | 초고속 (30초~1분) |
| 도구 | curl, axios, HTTP 클라이언트 |
| 대상 | Health Check, 주요 API, 인증/인가, DB 연결 |
| 실행 시점 | 개발 중 수동 실행 |
| 체크리스트 | YAML 파일 (scenarios/[project]/smoke.yaml) |

## 작업 순서

### 1단계: 환경 감지
```
├─ 현재 프로젝트 디렉토리 확인
├─ Docker Compose 실행 여부 확인
│   ├─ docker-compose ps로 컨테이너 상태 확인
│   └─ 미실행 시: 로컬 개발 서버 확인 (port 체크)
└─ baseURL 결정
    ├─ Docker Compose: http://localhost:3090
    ├─ 로컬 서버: http://localhost:[PORT]
    └─ 환경변수: $TEST_URL
```

**환경 감지 로직:**
1. `docker-compose ps` 실행하여 컨테이너 실행 여부 확인
2. 컨테이너 실행 중이면 Docker 환경으로 판단
3. 미실행 시 `netstat` 또는 `lsof`로 3090, 3000, 4000 포트 확인
4. baseURL 자동 설정 또는 사용자에게 확인 요청

### 2단계: YAML 체크리스트 로드
```
├─ scenarios/[project]/smoke.yaml 파일 읽기
├─ YAML 파싱 및 유효성 검증
│   ├─ 필수 필드 확인: name, type, checks[]
│   └─ 체크 항목 필드 확인: id, name, priority, request, expected
├─ 우선순위별 정렬
│   └─ critical → high → medium → low
└─ 체크 항목 개수 및 예상 소요 시간 계산
```

**YAML 구조 예시:**
```yaml
name: HubManager Smoke Test
type: SMOKE
timeout: 60000  # 1분 제한
retries: 1      # 실패 시 1회 재시도
checks:
  - id: health-check
    name: "Health Check API"
    priority: critical
    request:
      method: GET
      url: "/api/health"
      expected:
        status: 200
        body:
          contains: "ok"
```

### 3단계: 체크 실행 (순차적)
```
체크 항목별 루프:
├─ 📋 [1/5] Health Check API 실행 중...
├─ 🌐 HTTP 요청 전송
│   ├─ baseURL + url 조합
│   ├─ method, headers, body 설정
│   └─ timeout 적용 (기본 5초, YAML 설정 우선)
├─ ⏱️ 응답 대기 (timeout 내)
├─ ✅ 검증: status, body 확인
│   ├─ status 코드 비교
│   ├─ body.contains: 특정 문자열 포함 여부
│   ├─ body.hasKey: 특정 키 존재 여부
│   └─ body.isArray: 배열 여부
└─ 결과 기록
    ├─ 성공: ✅ + 응답시간 (ms)
    └─ 실패: ❌ + 에러 메시지

실패 시:
├─ ⚠️ 실패 원인 분석
│   ├─ 네트워크 오류 (ECONNREFUSED) → 서버 미실행
│   ├─ 타임아웃 (ETIMEDOUT) → 응답 지연
│   ├─ 잘못된 응답 (status 불일치) → API 오류
│   ├─ body 불일치 → 응답 형식 오류
│   └─ 인증 오류 (401/403) → 토큰 문제
├─ 🔧 자동 수정 시도
│   ├─ 서버 미실행 → Docker Compose 시작 제안
│   ├─ 타임아웃 → 재시도 (1회, retries 설정 따름)
│   └─ 기타 → 상세 로그 기록 + 다음 체크 진행
└─ 📊 실패 카운트 증가
```

**콘솔 출력 예시:**
```
🧪 스모크테스트 시작: WBHubManager
환경: Docker Compose (http://localhost:3090)
체크 항목: 5개

[1/5] ✅ Health Check API (120ms)
[2/5] ✅ JWT Public Key 조회 (85ms)
[3/5] ❌ Hub 목록 조회 (Timeout)
[4/5] ✅ JWT Token 발급 (340ms)
[5/5] ✅ DB 연결 확인 (45ms)

📊 결과 요약:
  - 총 체크: 5개
  - 성공: 4개 (80%)
  - 실패: 1개 (20%)
  - 실행 시간: 45초
```

### 4단계: 디버깅 및 원인 분석
```
실패한 체크에 대해:
├─ 📸 요청/응답 전체 로그 저장
│   ├─ 요청: method, url, headers, body
│   └─ 응답: status, headers, body (최대 1000자)
├─ 🔍 원인 분석 (5가지 카테고리)
│   ├─ 네트워크 오류: 서버 미실행, 포트 막힘
│   ├─ 타임아웃: 응답 지연, 성능 문제
│   ├─ 응답 오류: status 불일치, API 오류
│   ├─ body 불일치: 응답 형식 변경, 데이터 없음
│   └─ 인증 오류: 토큰 만료, 권한 부족
├─ 💡 해결책 제안
│   ├─ 서버 재시작: docker-compose restart
│   ├─ 환경변수 확인: JWT_SECRET, DATABASE_URL
│   ├─ DB 마이그레이션: npm run migrate
│   └─ 코드 수정: API 엔드포인트 확인
└─ 🔧 자동 수정 가능 시 적용
    └─ 서버 미실행 → docker-compose up -d 실행 시도
```

**실패 원인 카테고리:**
| 카테고리 | 에러 코드/메시지 | 해결책 |
|---------|-----------------|-------|
| 네트워크 오류 | ECONNREFUSED, ENOTFOUND | Docker Compose 시작 또는 포트 확인 |
| 타임아웃 | ETIMEDOUT, ESOCKETTIMEDOUT | 재시도 또는 timeout 증가 |
| 응답 오류 | Expected 200, got 500 | 서버 로그 확인, API 코드 검토 |
| body 불일치 | Expected "ok", got "error" | 응답 형식 확인, DB 데이터 확인 |
| 인증 오류 | 401 Unauthorized, 403 Forbidden | 토큰 재발급, 환경변수 확인 |

### 5단계: 리포트 생성
```
템플릿: /home/peterchung/HWTestAgent/test-plans/templates/테스트-리포트-템플릿.md

리포트 구조:
├─ 📊 스모크테스트 결과 요약
│   ├─ 프로젝트: WBHubManager
│   ├─ 환경: Docker Compose (localhost:3090)
│   ├─ 실행 시간: 2026-01-04 14:30:25
│   ├─ 총 체크 항목: 5개
│   ├─ 성공: 4개 (80%)
│   ├─ 실패: 1개 (20%)
│   └─ 총 실행시간: 45초
├─ 📋 체크 항목별 상세 결과 (테이블)
│   | 순번 | 체크 항목 | 우선순위 | 결과 | 응답시간 | 비고 |
│   |------|----------|---------|------|---------|------|
│   | 1 | Health Check API | 🔴 Critical | ✅ | 120ms | - |
│   | 2 | JWT Public Key | 🔴 Critical | ✅ | 85ms | - |
│   | 3 | Hub 목록 조회 | 🟠 High | ❌ | Timeout | 재시도 1회 실패 |
│   | 4 | JWT Token 발급 | 🔴 Critical | ✅ | 340ms | - |
│   | 5 | DB 연결 확인 | 🔴 Critical | ✅ | 45ms | - |
├─ ❌ 실패 케이스 상세 분석
│   ├─ 체크 항목: Hub 목록 조회
│   ├─ 실패 원인: ETIMEDOUT - 응답 지연 (5초 초과)
│   ├─ 요청 로그:
│   │   - method: GET
│   │   - url: http://localhost:3090/api/hubs
│   │   - headers: { ... }
│   ├─ 응답 로그: (없음 - timeout)
│   └─ 제안된 해결책:
│       1. DB 쿼리 성능 확인 (EXPLAIN ANALYZE)
│       2. 인덱스 추가 고려
│       3. timeout 설정 증가 (5초 → 10초)
├─ 🔧 수정사항 (자동 수정이 있을 경우만 표시)
│   └─ (없음)
└─ 📝 결론 및 권장사항
    ├─ 80% 통과율로 기본 기능은 정상
    ├─ Hub 목록 조회 성능 개선 필요
    └─ 다음 배포 전 재테스트 권장
```

### 6단계: DB 저장 (선택적)
```
TestAgent DB 저장:
├─ TestRun 테이블
│   ├─ test_type: 'SMOKE'
│   ├─ project: 'WBHubManager'
│   ├─ environment: 'docker' | 'local' | 'oracle'
│   ├─ status: 'passed' | 'failed'
│   ├─ total_checks: 5
│   ├─ passed_checks: 4
│   ├─ failed_checks: 1
│   ├─ duration: 45000 (ms)
│   ├─ base_url: 'http://localhost:3090'
│   └─ created_at: '2026-01-04T14:30:25Z'
├─ TestStep 테이블 (각 체크 항목별)
│   ├─ test_run_id: (foreign key)
│   ├─ step_number: 1
│   ├─ step_name: 'Health Check API'
│   ├─ priority: 'critical'
│   ├─ status: 'passed' | 'failed'
│   ├─ response_time: 120 (ms)
│   ├─ error_message: null (또는 에러 메시지)
│   └─ request_url: '/api/health'
└─ ErrorPattern 테이블 (실패 시만)
    ├─ test_run_id: (foreign key)
    ├─ pattern: 'timeout'
    ├─ error_code: 'ETIMEDOUT'
    ├─ suggestion: 'Check DB query performance or increase timeout'
    └─ count: 1
```

**DB 저장 조건:**
- TestAgent DB 연결이 가능한 경우에만 저장
- 연결 실패 시 로컬 파일로만 저장 (에러 무시)
- 환경변수 `TESTAGENT_DB_URL`로 DB URL 설정

### 7단계: 로컬 파일 저장
```
리포트: /home/peterchung/HWTestAgent/test-results/MyTester/reports/
        YYYY-MM-DD-[프로젝트명]-스모크-테스트.md

로그 (JSON): /home/peterchung/HWTestAgent/test-results/MyTester/logs/
             YYYY-MM-DD-[프로젝트명]-smoke-details.json
```

**로그 JSON 구조:**
```json
{
  "project": "WBHubManager",
  "environment": "docker",
  "baseUrl": "http://localhost:3090",
  "startTime": "2026-01-04T14:30:25Z",
  "endTime": "2026-01-04T14:31:10Z",
  "duration": 45000,
  "summary": {
    "total": 5,
    "passed": 4,
    "failed": 1,
    "passRate": 80
  },
  "checks": [
    {
      "id": "health-check",
      "name": "Health Check API",
      "priority": "critical",
      "status": "passed",
      "responseTime": 120,
      "request": {
        "method": "GET",
        "url": "/api/health"
      },
      "response": {
        "status": 200,
        "body": { "status": "ok" }
      }
    },
    {
      "id": "hub-list",
      "name": "Hub 목록 조회",
      "priority": "high",
      "status": "failed",
      "responseTime": null,
      "error": {
        "code": "ETIMEDOUT",
        "message": "Request timeout after 5000ms"
      },
      "retries": 1
    }
  ]
}
```

## 자동 수정 대상

| 오류 유형 | 감지 방법 | 자동 수정 방법 |
|----------|----------|--------------|
| 서버 미실행 | ECONNREFUSED | `docker-compose up -d` 실행 시도 |
| 타임아웃 | ETIMEDOUT | 재시도 (retries 설정 따름) |
| 응답 오류 | status 500/502/503 | 로그 출력 + 수동 확인 요청 |
| body 불일치 | expected vs actual 비교 | 로그 출력 + 수동 확인 요청 |
| 인증 오류 | 401/403 | 토큰 재발급 시도 (가능 시) |

**자동 수정 제한:**
- 최대 1회만 자동 수정 시도 (무한 루프 방지)
- Docker Compose 시작은 사용자 확인 후 실행
- 코드 수정은 자동으로 하지 않음

## 사용 예시

### 기본 사용
```bash
/스킬테스터 스모크
/스킬테스터 허브매니저 스모크
/스킬테스터 핀허브 smoke test
```

### 환경 지정
```bash
/스킬테스터 로컬에서 세일즈허브 스모크
/스킬테스터 오라클 허브매니저 필수체크
```

### 특정 체크만 실행 (선택사항)
```bash
/스킬테스터 허브매니저 스모크 --only health-check,jwt-public-key
```

## 파싱 정보 (메인에서 전달)

```typescript
interface SmokeTestConfig {
  project: string;              // 프로젝트명: WBHubManager, WBFinHub 등
  projectPath: string;          // 프로젝트 경로
  env: 'local' | 'docker' | 'oracle' | 'railway';  // 실행 환경
  baseUrl?: string;             // 명시적 baseURL (선택)
  yamlPath?: string;            // YAML 파일 경로 (기본: scenarios/[project]/smoke.yaml)
  maxRetries: number;           // 최대 재시도 횟수 (기본: 1)
  timeout: number;              // 개별 체크 timeout (기본: 5000ms)
  saveDb: boolean;              // DB 저장 여부 (기본: true)
  autoFix: boolean;             // 자동 수정 시도 여부 (기본: true)
}
```

## 참조 파일

### YAML 체크리스트 예시
- `/home/peterchung/HWTestAgent/scenarios/wbhubmanager/smoke.yaml`
- `/home/peterchung/HWTestAgent/scenarios/wbhubmanager/precision.yaml` (참조용)

### TestAgent DB 모델
- `/home/peterchung/HWTestAgent/src/storage/models/TestRun.ts`
- `/home/peterchung/HWTestAgent/src/storage/models/TestStep.ts`
- `/home/peterchung/HWTestAgent/src/storage/models/ErrorPattern.ts`

### 리포트 템플릿
- `/home/peterchung/HWTestAgent/test-plans/templates/테스트-리포트-템플릿.md`

### 기존 스킬 참조
- `~/.claude/skills/스킬테스터-단위.md` - 단위 테스트 패턴
- `~/.claude/skills/스킬테스터-E2E.md` - E2E 테스트 패턴
