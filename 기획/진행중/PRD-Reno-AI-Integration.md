# PRD: Reno AI 기능 프론트엔드 통합 및 자동화

## 1. Introduction/Overview

### 현재 상황
WBSalesHub의 Reno AI 모듈에는 세 가지 핵심 AI 기능이 이미 **백엔드 100% 완성** 상태로 구현되어 있습니다:

1. **Meeting Prep Generator** - 미팅 전 고객 컨텍스트 자동 정리
2. **Message Drafter** - 팔로업 메일 초안 자동 작성
3. **Deal Health Analyzer** - 고객 온도(engagement) 지표화 및 위험 고객 감지

**백엔드 구현 현황**:
- ✅ API 엔드포인트: 모두 등록됨
- ✅ 데이터베이스 스키마: 완벽히 구축됨
- ✅ Claude API 연동: 정상 작동
- ✅ 비즈니스 로직: 완성도 100%

**프론트엔드 통합 현황**:
- ❌ `AISidebar` 컴포넌트가 어떤 페이지에도 연결되지 않음
- ❌ 자동 트리거 없음 (사용자가 수동으로 ID 입력해야 함)
- ❌ 대시보드/고객 상세 페이지에 통합 안 됨
- ❌ 자동화 기능(크론 작업, Slack 알림) 없음

### 해결하려는 문제
**"구현은 완벽하지만 사용자가 접근할 수 없는 상태"**

현재 Reno AI 기능은 기술적으로 완벽히 작동하지만, 실제 사용자 워크플로우에 통합되지 않아 사실상 사용 불가능합니다. 영업 담당자가 미팅 준비, 팔로업 메일 작성, 위험 고객 모니터링 등의 업무에서 AI의 도움을 받을 수 없는 상태입니다.

### 목표
이미 구현된 세 가지 AI 기능을 실제 사용자 플로우에 통합하고 자동화를 추가하여, 영업 담당자가 일상 업무에서 자연스럽게 AI의 도움을 받을 수 있도록 만듭니다.

---

## 2. Goals

### 비즈니스 목표
1. **영업 생산성 향상**: 미팅 준비 시간 50% 단축
2. **고객 이탈 예방**: 위험 고객 조기 감지 및 대응
3. **팔로업 품질 개선**: AI 기반 이메일 초안으로 일관된 커뮤니케이션
4. **데이터 기반 의사결정**: 고객 건강도 지표로 우선순위 설정

### 기술 목표
1. AISidebar를 주요 페이지(대시보드, 고객 상세, 미팅 상세)에 통합
2. 자동 트리거 구현으로 수동 입력 단계 제거
3. 크론 작업으로 자동 브리핑 생성 및 Slack 알림 구현
4. 기존 컴포넌트 최대한 재사용하여 개발 시간 단축

---

## 3. User Stories

### Story 1: 미팅 전 자동 브리핑
**As a** 영업 담당자
**I want to** 예정된 미팅의 상세 페이지를 열 때 자동으로 고객 브리핑을 볼 수 있다
**So that** 별도로 정보를 찾지 않아도 미팅 준비가 완료된다

**Acceptance Criteria**:
- [ ] 미팅 상세 페이지에 "AI 브리핑" 탭 자동 표시
- [ ] 고객 요약, 최근 상호작용 10개, 제안 주제 자동 생성
- [ ] 로딩 상태 표시 (3-5초 소요)
- [ ] 브리핑 새로고침 버튼
- [ ] 지난 미팅 노트 요약 포함

### Story 2: 미팅노트 작성 후 팔로업 메일 자동 생성
**As a** 영업 담당자
**I want to** 미팅노트 저장 후 "팔로업 메일 생성" 버튼을 클릭하여 초안을 받을 수 있다
**So that** 빠르게 팔로업 이메일을 작성할 수 있다

**Acceptance Criteria**:
- [ ] 미팅노트 상세 모달 헤더에 "팔로업 메일 생성" 버튼 추가
- [ ] 버튼 클릭 시 미팅노트 내용 기반 이메일 초안 생성
- [ ] 제목, 본문, 대안 표현, 작성 팁 제공
- [ ] 초안 복사 버튼
- [ ] 톤 선택 옵션 (formal, semi-formal, casual)

### Story 3: 대시보드에서 위험 고객 조회
**As a** 영업 관리자
**I want to** 대시보드에서 건강도 점수가 낮은 위험 고객 목록을 볼 수 있다
**So that** 조기에 대응할 수 있다

**Acceptance Criteria**:
- [ ] 대시보드에 "주의 필요 고객" 위젯 추가
- [ ] 건강도 50점 미만 고객 표시 (최대 10개)
- [ ] 점수, 트렌드(improving/stable/declining), 고객명 표시
- [ ] 클릭 시 고객 상세 페이지로 이동
- [ ] 마지막 상호작용 일자 표시

### Story 4: 고객 상세 페이지에서 자동 건강도 표시
**As a** 영업 담당자
**I want to** 고객 상세 페이지를 열 때 자동으로 거래 건강도를 볼 수 있다
**So that** 고객 상태를 한눈에 파악할 수 있다

**Acceptance Criteria**:
- [ ] 고객 상세 페이지 헤더에 건강도 점수 배지 표시
- [ ] 점수, 트렌드, 5가지 건강 요인 시각화
- [ ] AI 추천사항 표시 (예: "2주간 연락 없음. 이메일 팔로업 권장")
- [ ] 점수 재계산 버튼
- [ ] 색상 코딩: 0-50 (빨강), 51-70 (주황), 71-85 (노랑), 86-100 (초록)

### Story 5: 자동 미팅 브리핑 생성 (크론)
**As a** 시스템
**I want to** 예정된 미팅 24시간 전에 자동으로 브리핑을 생성한다
**So that** 담당자가 미팅 페이지를 열 때 이미 준비되어 있다

**Acceptance Criteria**:
- [ ] 매일 자정 크론 작업 실행
- [ ] 내일 예정된 모든 미팅의 브리핑 자동 생성
- [ ] 생성 결과 로그 저장 (`cron_logs` 테이블)
- [ ] 실패 시 에러 로깅 및 재시도 (최대 3회)
- [ ] 크론 작업 상태 모니터링 엔드포인트

### Story 6: 위험 고객 Slack 알림
**As a** 영업 담당자
**I want to** 담당 고객의 건강도 점수가 50점 이하로 떨어지면 Slack으로 알림을 받는다
**So that** 즉시 대응할 수 있다

**Acceptance Criteria**:
- [ ] 매일 오전 9시 크론 작업 실행
- [ ] 모든 고객의 건강도 스캔
- [ ] 50점 미만 고객의 담당자에게 Slack DM 발송
- [ ] 알림 메시지: 고객명, 점수, 주요 이슈, 추천 액션 포함
- [ ] 이미 알림을 받은 고객은 점수가 10점 이상 변동될 때만 재알림

---

## 4. Functional Requirements

### FR-1: AISidebar 통합
- 대시보드 레이아웃 (`app/(dashboard)/layout.tsx`)에 AISidebar 추가
- 고객 상세 페이지 레이아웃에 AISidebar 추가
- 미팅 상세 페이지 레이아웃에 AISidebar 추가
- 우측 상단 토글 버튼으로 열기/닫기 (기본값: 닫힘)
- 사용자별 상태 저장 (localStorage)

### FR-2: 미팅 상세 페이지 자동 브리핑
- `MeetingDetail` 컴포넌트에 "AI 브리핑" 탭 추가
- 페이지 로드 시 자동으로 `GET /api/reno/meeting-prep/:meetingId` 호출
- 브리핑 데이터 시각화 (기존 `MeetingPrepPanel` 컴포넌트 재사용)
- 새로고침 버튼으로 브리핑 재생성
- 로딩 상태: Skeleton UI 또는 스피너

### FR-3: 미팅노트 상세 모달 팔로업 버튼
- `MeetingNoteDetail` 컴포넌트 헤더에 "팔로업 메일 생성" 버튼 추가
- 버튼 위치: 수정/삭제 버튼 옆
- 버튼 클릭 시 팔로업 메일 생성 모달 오픈
- `POST /api/reno/message-draft` 호출
  - `templateType: 'follow_up'`
  - `customerId`: 연동된 고객 ID
  - `context: { meetingNoteId, meetingNoteContent }`
- 기존 `MessageDraftPanel` 컴포넌트 재사용하여 결과 표시

### FR-4: 대시보드 위험 거래 위젯
- `Dashboard` 페이지에 `DealsAtRiskWidget` 컴포넌트 추가 (신규 작성)
- `GET /api/reno/deals-at-risk` 호출
- 테이블 형식으로 표시:
  - 고객명
  - 건강도 점수 (색상 코딩)
  - 트렌드 아이콘 (↑ improving, → stable, ↓ declining)
  - 마지막 상호작용 일자
- 클릭 시 고객 상세 페이지로 이동
- 빈 상태 메시지: "모든 고객이 건강한 상태입니다! 🎉"

### FR-5: 고객 상세 페이지 자동 건강도
- `CustomerDetail` 컴포넌트 헤더에 `DealHealthBadge` 컴포넌트 추가 (신규 작성)
- 페이지 로드 시 자동으로 `GET /api/reno/deal-health/:customerId` 호출
- 점수 배지 표시 (우측 상단)
- 툴팁에 상세 정보:
  - 5가지 건강 요인 점수 (Engagement, Timeline, Stage, Budget, Response Rate)
  - 트렌드
  - AI 추천사항 (예: "2주간 연락 없음. 이메일 팔로업 권장")
- 배지 클릭 시 AISidebar의 `DealHealthPanel` 오픈
- 점수 재계산 버튼

### FR-6: 고객 목록 건강도 컬럼
- `CustomerTable` 컴포넌트에 "건강도" 컬럼 추가
- 각 고객의 최신 건강도 점수 표시
- 백엔드 API 확장:
  - `GET /api/customers?includeHealthScore=true` 쿼리 파라미터 추가
  - 고객 목록 조회 시 최신 건강도 점수 JOIN
- 색상 코딩: 0-50 (빨강), 51-70 (주황), 71-85 (노랑), 86-100 (초록)
- 정렬 가능 (오름차순/내림차순)

### FR-7: 자동 미팅 브리핑 크론
**백엔드 신규 파일 생성**:
- `server/cron/index.ts` - 크론 스케줄러 초기화
- `server/cron/generateUpcomingMeetingBriefings.ts` - 미팅 브리핑 크론 작업

**구현 내용**:
- `node-cron` 패키지 사용
- 스케줄: 매일 자정 (`0 0 * * *`)
- 로직:
  1. 내일 예정된 모든 미팅 조회
  2. 각 미팅에 대해 `MeetingPrepGenerator.generateBriefing()` 호출
  3. 생성 결과 로그 저장 (`cron_logs` 테이블)
- 실패 시 재시도 (최대 3회)
- 환경변수 `CRON_ENABLED=true`일 때만 실행

### FR-8: 위험 고객 Slack 알림 크론
**백엔드 신규 파일 생성**:
- `server/cron/notifyDealsAtRisk.ts` - 위험 고객 알림 크론 작업

**구현 내용**:
- 스케줄: 매일 오전 9시 (`0 9 * * *`)
- 로직:
  1. `DealHealthAnalyzer.getDealsAtRisk()` 호출 (score < 50)
  2. 각 위험 고객의 담당자 이메일 → Slack User ID 매핑
  3. Slack DM 발송 (기존 `@slack/bolt` 활용)
  4. 알림 이력 저장 (`notification_history` 테이블)
- 중복 알림 방지: 24시간 내 동일 고객 알림 스킵 (점수 변동 10점 이상일 때만 재알림)
- Slack 메시지 포맷:
  ```
  🚨 고객 주의 알림

  **고객명**: [고객명]
  **건강도 점수**: [점수]/100 (↓ declining)
  **마지막 접촉**: [일자] ([X]일 전)

  **주요 이슈**:
  - 2주간 상호작용 없음
  - 예상 종료일까지 30일 남음

  **추천 액션**:
  - 이메일 팔로업 권장
  - 미팅 일정 재조정 검토

  [고객 상세 보기]
  ```

---

## 5. Non-Goals (Out of Scope)

### 초기 버전에서 제외되는 사항
1. **새로운 AI 기능 추가**: 백엔드 로직 변경 없음 (기존 기능 활용)
2. **고객 컨텍스트 수동 편집 UI**: 이미 `CustomerContextManagementTab`에 구현되어 있음
3. **다국어 지원**: 한국어만 지원
4. **모바일 최적화**: 데스크톱 우선
5. **이메일 직접 발송 기능**: 초안만 제공 (복사 후 수동 발송)
6. **고급 건강도 분석**: 기존 5가지 요인으로 충분 (추가 요인 확장 없음)
7. **커스텀 알림 설정**: 담당자별로 알림 규칙 변경 불가 (공통 규칙 적용)
8. **대화형 AI 챗봇**: 단방향 정보 제공만 (양방향 대화 없음)

---

## 6. Design Considerations

### UI/UX 요구사항

#### AISidebar
- **위치**: 화면 우측, 고정 너비 400px
- **토글 버튼**: 우측 상단 플로팅 버튼 (아이콘: Sparkles ✨)
- **애니메이션**: 슬라이드 인/아웃 (300ms)
- **기본 상태**: 닫힘 (localStorage에 상태 저장)

#### DealsAtRiskWidget (대시보드)
- **위치**: 대시보드 상단, 전체 너비
- **레이아웃**: 카드 형태, 그림자 효과
- **테이블 헤더**: 고객명, 건강도, 트렌드, 마지막 접촉
- **빈 상태**: "모든 고객이 건강한 상태입니다! 🎉" + 일러스트

#### DealHealthBadge (고객 상세)
- **위치**: 고객 상세 페이지 헤더 우측 상단
- **크기**: 작은 배지 (점수 + 색상)
- **툴팁**: 호버 시 상세 정보 표시 (5가지 요인, 추천사항)
- **색상**: 0-50 (빨강), 51-70 (주황), 71-85 (노랑), 86-100 (초록)

#### FollowUpEmailButton (미팅노트)
- **위치**: 미팅노트 상세 모달 헤더, 수정/삭제 버튼 옆
- **아이콘**: Mail 아이콘 + "팔로업 메일 생성" 텍스트
- **모달**: 중앙 팝업, 최대 너비 800px
- **내용**: 제목, 본문, 대안 표현, 작성 팁, 복사 버튼

#### 건강도 컬럼 (고객 목록)
- **너비**: 100px
- **정렬**: 중앙 정렬
- **색상**: 배경색으로 점수 시각화
- **툴팁**: 호버 시 트렌드 표시

### 재사용 컴포넌트
- `/frontend/components/ui/dialog.tsx`
- `/frontend/components/ui/button.tsx`
- `/frontend/components/ui/badge.tsx`
- `/frontend/components/ui/skeleton.tsx`
- `/frontend/components/reno/AISidebar.tsx` (기존)
- `/frontend/components/reno/deal-health/DealHealthPanel.tsx` (기존)
- `/frontend/components/reno/meeting-prep/MeetingPrepPanel.tsx` (기존)
- `/frontend/components/reno/message-drafting/MessageDraftPanel.tsx` (기존)

---

## 7. Technical Considerations

### 백엔드 변경 사항

#### 신규 파일
1. **크론 스케줄러**:
   - `server/cron/index.ts` - 크론 초기화 및 스케줄링
   - `server/cron/generateUpcomingMeetingBriefings.ts` - 미팅 브리핑 크론
   - `server/cron/notifyDealsAtRisk.ts` - 위험 고객 알림 크론

2. **데이터베이스 마이그레이션**:
   - `server/database/migrations/015_add_cron_tables.sql`

#### 기존 파일 수정
1. **server/index.ts**:
   - 크론 초기화 추가 (`setupCronJobs()`)
   - 환경변수 `CRON_ENABLED` 체크

2. **server/routes/customerRoutes.ts**:
   - `GET /api/customers` 엔드포인트에 `includeHealthScore` 쿼리 파라미터 추가
   - 고객 목록 조회 시 `deal_health_scores` 테이블 LEFT JOIN

3. **server/routes/renoRoutes.ts** (선택 사항):
   - `GET /api/reno/cron/logs` - 크론 로그 조회 엔드포인트 (관리자 전용)

#### 데이터베이스 스키마 추가
```sql
-- 크론 작업 로그
CREATE TABLE cron_logs (
  id SERIAL PRIMARY KEY,
  job_name VARCHAR(100) NOT NULL,
  status VARCHAR(20) NOT NULL CHECK (status IN ('success', 'failed', 'running')),
  message TEXT,
  records_processed INT DEFAULT 0,
  started_at TIMESTAMP NOT NULL,
  completed_at TIMESTAMP,
  error_message TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 알림 이력
CREATE TABLE notification_history (
  id SERIAL PRIMARY KEY,
  customer_id UUID NOT NULL,
  notification_type VARCHAR(50) NOT NULL,
  channel VARCHAR(20) NOT NULL CHECK (channel IN ('slack', 'email')),
  recipient VARCHAR(255) NOT NULL,
  message TEXT NOT NULL,
  sent_at TIMESTAMP DEFAULT NOW(),
  is_delivered BOOLEAN DEFAULT TRUE,
  deal_health_score INT,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_cron_logs_job_started ON cron_logs(job_name, started_at DESC);
CREATE INDEX idx_notification_history_customer ON notification_history(customer_id, sent_at DESC);
```

#### Slack 통합
- 기존 `@slack/bolt` 패키지 활용
- 사용자 이메일 → Slack User ID 매핑 필요:
  ```typescript
  async function getSlackUserIdByEmail(email: string): Promise<string | null> {
    const result = await slackClient.users.lookupByEmail({ email });
    return result.ok ? result.user.id : null;
  }
  ```

#### 환경변수
```bash
# 기존
ANTHROPIC_API_KEY=sk-ant-...

# 신규 추가
CRON_ENABLED=true  # 크론 활성화 플래그 (기본값: true)
SLACK_BOT_TOKEN=xoxb-...  # Slack 알림용
```

### 프론트엔드 변경 사항

#### 신규 파일
1. **컴포넌트**:
   - `frontend/components/reno/DealsAtRiskWidget.tsx` - 위험 거래 위젯
   - `frontend/components/reno/DealHealthBadge.tsx` - 건강도 배지
   - `frontend/components/reno/FollowUpEmailButton.tsx` - 팔로업 버튼

#### 기존 파일 수정
1. **레이아웃**:
   - `frontend/app/(dashboard)/layout.tsx` - AISidebar 추가

2. **페이지**:
   - `frontend/app/(dashboard)/page.tsx` - DealsAtRiskWidget 추가

3. **컴포넌트**:
   - `frontend/components/customers/CustomerDetail.tsx` - DealHealthBadge 추가
   - `frontend/components/customers/CustomerTable.tsx` - 건강도 컬럼 추가
   - `frontend/components/meetings/MeetingDetail.tsx` - 브리핑 탭 추가
   - `frontend/components/meeting-notes/MeetingNoteDetail.tsx` - 팔로업 버튼 추가

#### API 클라이언트
- `frontend/services/renoApi.ts` (신규 또는 기존 확장):
  ```typescript
  export async function getDealHealth(customerId: string) {
    return await fetch(`/api/reno/deal-health/${customerId}`);
  }

  export async function getDealsAtRisk() {
    return await fetch('/api/reno/deals-at-risk');
  }

  export async function getMeetingPrep(meetingId: string) {
    return await fetch(`/api/reno/meeting-prep/${meetingId}`);
  }

  export async function generateFollowUpDraft(customerId: string, context: any) {
    return await fetch('/api/reno/message-draft', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        templateType: 'follow_up',
        customerId,
        context
      })
    });
  }
  ```

---

## 8. Success Metrics

### 정량적 지표
1. **AISidebar 사용률**: 월 활성 사용자의 70% 이상
2. **미팅 브리핑 조회율**: 전체 미팅의 50% 이상 (페이지 로드 시 자동 조회)
3. **팔로업 메일 생성률**: 미팅노트의 30% 이상에서 팔로업 버튼 클릭
4. **위험 고객 조기 대응률**: Slack 알림 후 24시간 내 액션 기록 80% 이상
5. **평균 미팅 준비 시간**: 기존 대비 50% 단축 (20분 → 10분)
6. **고객 이탈률 감소**: 위험 고객의 20% 이상 복구

### 정성적 지표
1. **사용자 만족도**: AI 기능에 대한 피드백 수집 (NPS 80 이상 목표)
2. **업무 효율성**: "AI 덕분에 시간이 절약됐다" 피드백 70% 이상
3. **데이터 품질**: 고객 건강도 지표가 실제 상황과 일치하는 비율 90% 이상

---

## 9. Implementation Plan

### Phase 1: 기본 통합 (3일)
**목표**: 사용자가 AI 기능에 접근할 수 있도록 함

**작업 항목**:
1. **Day 1**: AISidebar 통합
   - `app/(dashboard)/layout.tsx`에 AISidebar 추가
   - 토글 버튼 구현
   - localStorage 상태 저장
   - 테스트: 대시보드, 고객 상세, 미팅 상세 페이지에서 AISidebar 열기/닫기

2. **Day 2**: 고객 상세 페이지 자동 건강도
   - `DealHealthBadge.tsx` 컴포넌트 생성
   - `CustomerDetail.tsx`에 배지 추가
   - 페이지 로드 시 API 호출 구현
   - 툴팁 UI 구현
   - 테스트: 건강도 점수 자동 표시 및 색상 코딩 확인

3. **Day 3**: 대시보드 위험 거래 위젯
   - `DealsAtRiskWidget.tsx` 컴포넌트 생성
   - `Dashboard.tsx`에 위젯 추가
   - API 연동 및 테이블 렌더링
   - 빈 상태 처리
   - 테스트: 위험 고객 목록 표시 및 클릭 시 이동 확인

**검증**:
- [ ] AISidebar 토글 작동 (모든 페이지)
- [ ] 고객 상세 페이지에서 건강도 배지 표시
- [ ] 대시보드 위젯 렌더링
- [ ] 색상 코딩 정확성 확인

### Phase 2: 자동 트리거 (2일)
**목표**: 수동 입력 제거, 자동으로 컨텍스트 전달

**작업 항목**:
4. **Day 4**: 미팅 상세 페이지 자동 브리핑
   - `MeetingDetail.tsx`에 "AI 브리핑" 탭 추가
   - 페이지 로드 시 자동 API 호출
   - `MeetingPrepPanel` 컴포넌트 재사용
   - 로딩 상태 및 에러 처리
   - 테스트: 미팅 페이지 열 때 자동 브리핑 생성

5. **Day 5**: 미팅노트 팔로업 버튼 + 고객 목록 건강도 컬럼
   - `FollowUpEmailButton.tsx` 컴포넌트 생성
   - `MeetingNoteDetail.tsx`에 버튼 추가
   - 팔로업 메일 생성 모달 구현
   - `CustomerTable.tsx`에 건강도 컬럼 추가
   - 백엔드: `GET /api/customers?includeHealthScore=true` 구현
   - 테스트: 팔로업 버튼 클릭 → 초안 생성, 고객 목록 컬럼 표시

**검증**:
- [ ] 미팅 페이지 자동 브리핑 생성
- [ ] 미팅노트 팔로업 버튼 작동
- [ ] 고객 목록 건강도 컬럼 표시 및 정렬

### Phase 3: 자동화 (2일)
**목표**: 크론 작업으로 사전 대응

**작업 항목**:
6. **Day 6**: 자동 미팅 브리핑 크론 + DB 마이그레이션
   - DB 마이그레이션 실행 (`015_add_cron_tables.sql`)
   - `server/cron/index.ts` 생성
   - `server/cron/generateUpcomingMeetingBriefings.ts` 생성
   - `server/index.ts`에서 크론 초기화
   - 테스트: 크론 작업 수동 트리거 (내일 미팅 브리핑 생성)

7. **Day 7**: 위험 고객 Slack 알림 크론
   - `server/cron/notifyDealsAtRisk.ts` 생성
   - Slack 메시지 포맷 작성
   - 이메일 → Slack User ID 매핑 구현
   - 중복 알림 방지 로직
   - 테스트: 크론 작업 수동 트리거, Slack DM 수신 확인

**검증**:
- [ ] 크론 작업 수동 실행 성공
- [ ] `cron_logs` 테이블에 로그 저장
- [ ] Slack 알림 수신 확인
- [ ] `notification_history` 테이블에 이력 저장

### Phase 4: 테스트 및 배포 (1일)
**작업 항목**:
8. **Day 8**: E2E 테스트 + 프로덕션 배포
   - Playwright E2E 테스트 작성:
     - AISidebar 토글
     - 건강도 자동 표시
     - 브리핑 생성
     - 팔로업 버튼
   - 환경변수 설정 확인 (`ANTHROPIC_API_KEY`, `SLACK_BOT_TOKEN`, `CRON_ENABLED`)
   - Docker 이미지 빌드
   - 프로덕션 배포
   - 크론 작업 활성화 확인

**검증**:
- [ ] 모든 E2E 테스트 통과
- [ ] 프로덕션 환경에서 정상 작동
- [ ] 크론 작업 스케줄링 확인 (로그 모니터링)

---

## 10. Open Questions

### 기술적 질문
1. ✅ **고객 목록 건강도 조회 방식**: JOIN vs 개별 API 호출
   - **결정**: JOIN 방식 (성능상 유리)
   - `GET /api/customers?includeHealthScore=true` 쿼리 파라미터

2. ✅ **크론 작업 실패 시 재시도 로직**: 최대 3회 재시도
   - 1분 간격으로 재시도
   - 3회 실패 시 에러 로그 저장 및 알림

3. ✅ **Slack 알림 중복 방지**: 24시간 내 동일 고객 알림 스킵
   - 예외: 점수 변동 10점 이상일 때만 재알림

4. ⚠️ **이메일 → Slack User ID 매핑 실패 시**: 기본 채널에 알림?
   - **미결정**: 실패 시 에러 로그만 저장 vs 대체 채널 알림

### 비즈니스 질문
5. ✅ **위험 고객 기준**: 건강도 50점 미만
   - 향후 관리자 설정으로 변경 가능하도록 확장

6. ⚠️ **팔로업 메일 템플릿 커스터마이징**: 사용자별 템플릿 저장?
   - **미결정**: Phase 1에서는 공통 템플릿만, 향후 개인화 기능 추가 고려

### UX 질문
7. ✅ **AISidebar 기본 상태**: 닫힘
   - 사용자가 명시적으로 열어야 함 (화면 공간 확보)

8. ⚠️ **건강도 점수 갱신 주기**: 실시간 vs 일일 배치
   - **미결정**: Phase 1에서는 페이지 로드 시 실시간 조회, 향후 캐싱 고려

---

## 11. Risk Management

| 리스크 | 영향도 | 대응 방안 |
|--------|--------|----------|
| Claude API 비용 초과 | High | 월간 예산 설정 ($200), 초과 시 알림 |
| 크론 작업 실패 | Medium | 재시도 로직 + 에러 로깅 + 모니터링 |
| Slack User ID 매핑 실패 | Medium | 에러 로그 저장, 대체 알림 방식 검토 |
| 건강도 점수 부정확성 | High | 실제 데이터로 검증, 피드백 수집 후 가중치 조정 |
| 프론트엔드 성능 저하 (API 호출 증가) | Low | 캐싱 전략 (React Query), API 응답 최적화 |
| 사용자 저항 (AI 신뢰도) | Medium | 사용 가이드 제공, 초기 피드백 수집 |

---

## 12. Future Enhancements (Phase 2+)

### Phase 2 후보 (우선순위 순)
1. **건강도 점수 캐싱**: 실시간 계산 대신 일일 배치로 사전 계산
2. **커스텀 알림 설정**: 담당자별로 알림 규칙 설정 (점수 기준, 알림 시간)
3. **주간 건강도 리포트**: 매주 월요일 이메일로 전체 고객 건강도 요약 발송
4. **팔로업 메일 개인화**: 사용자별 템플릿 저장 및 재사용
5. **대화형 AI 챗봇**: 고객 정보 질문/답변 (현재는 단방향 정보 제공만)
6. **건강도 히스토리 차트**: 시간에 따른 점수 변화 시각화
7. **다국어 지원**: 영어 메시지 생성
8. **모바일 최적화**: 반응형 UI 개선

### Long-term Vision
- **통합 AI 대시보드**: 모든 AI 기능을 한 곳에서 관리
- **예측 분석**: 고객 이탈 확률 예측 (머신러닝 모델)
- **자동 팔로업 발송**: AI가 생성한 메일을 사용자 승인 후 자동 발송
- **음성 미팅 노트**: Fireflies 연동 강화, 음성→텍스트→요약 자동화

---

**버전**: 1.0
**작성일**: 2026-01-24
**작성자**: Claude Sonnet 4.5
**관련 문서**:
- [PRD-Reno-AI-Agent.md](./PRD-Reno-AI-Agent.md) - 기존 Reno AI 아키텍처 (참고용)
- [기능-리스트.md](../문서/참고자료/기능-리스트.md) - 전체 기능 목록

---

**작업량 추정**: **8일 (64 WU)**
- Phase 1 (기본 통합): 3일 (24 WU)
- Phase 2 (자동 트리거): 2일 (16 WU)
- Phase 3 (자동화): 2일 (16 WU)
- Phase 4 (테스트/배포): 1일 (8 WU)
