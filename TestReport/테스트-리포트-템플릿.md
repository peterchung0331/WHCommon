# [프로젝트명] [테스트 유형] 테스트 리포트

**테스트 일시:** YYYY-MM-DD
**테스트 대상:** [테스트 대상 시스템/기능]
**테스트 환경:** [환경 설명 - Docker/Railway/로컬 등]
**최종 결과:** ✅/❌ **전체 통과/실패 (N/M, XX%)**

---

## Part 1: 테스트 결과 및 수정사항

### 📊 최종 테스트 결과

| # | 테스트 항목 | 결과 | 설명 |
|---|------------|------|------|
| 1 | [테스트 항목명] | ✅/❌ 통과/실패 | [테스트 설명] |
| 2 | [테스트 항목명] | ✅/❌ 통과/실패 | [테스트 설명] |
| 3 | [테스트 항목명] | ✅/❌ 통과/실패 | [테스트 설명] |

**통과율:** N/M (XX%)

---

### 🔧 주요 수정사항

#### 1. [수정사항 제목]
**파일:** [파일경로](../파일경로)

**문제:**
```언어
// 문제가 있던 코드
```

**수정:**
```언어
// 수정된 코드
```

**이유:** [수정 이유 설명]

---

#### 2. [수정사항 제목]
**파일:** [파일경로](../파일경로)

**문제:**
```언어
// 문제가 있던 코드
```

**수정:**
```언어
// 수정된 코드
```

**이유:** [수정 이유 설명]

---

### 📁 생성/수정된 파일 목록

#### 수정된 파일
1. [파일명](../파일경로) - [변경 내용 요약]
2. [파일명](../파일경로) - [변경 내용 요약]

#### 신규 생성 파일
1. [파일명](../파일경로) - [파일 목적]
2. [파일명](../파일경로) - [파일 목적]

---

### 🔍 발견된 문제점

#### 1. [문제점 제목]
**문제:** [문제 상세 설명]

**조치:** [취한 조치]

**권장사항:** [향후 개선 방안]

---

#### 2. [문제점 제목]
**현상:** [관찰된 현상]

**예상 원인:**
- [원인 1]
- [원인 2]

**영향도:** 높음/중간/낮음 ([영향 설명])

**권장사항:**
- [권장사항 1]
- [권장사항 2]

---

## Part 2: 테스트 케이스 유효성 평가 및 개선 제안

### 📋 테스트 케이스별 평가

#### Test 1: [테스트명]
**목적:** [테스트의 목적]

**유효성:** ⭐⭐⭐⭐⭐ (X/5)
- [유효성 평가 근거 1]
- [유효성 평가 근거 2]

**검증 항목:**
- ✅/❌ [검증 항목 1]
- ✅/❌ [검증 항목 2]

**개선 제안:**
```javascript
// 개선된 테스트 코드 예시
```

**우선순위:** 필수/높음/중간/낮음 (Critical/High/Medium/Low)

---

#### Test 2: [테스트명]
**목적:** [테스트의 목적]

**유효성:** ⭐⭐⭐⭐⭐ (X/5)
- [유효성 평가 근거]

**검증 항목:**
- ✅/❌ [검증 항목]

**개선 제안:**
```javascript
// 개선된 테스트 코드
```

**시나리오 추가 제안:**
- [추가 시나리오 1]
- [추가 시나리오 2]

**우선순위:** 필수/높음/중간/낮음

---

### 🎯 전체 테스트 시나리오 개선 제안

#### 1. End-to-End 통합 테스트 추가
**현재 한계:** [현재 테스트의 한계점]

**제안:**
```javascript
async function testCompleteFlow() {
  // 1. [단계 1]

  // 2. [단계 2]

  // 3. [단계 3]

  console.log('✅ Complete flow test PASSED');
  return true;
}
```

---

#### 2. 보안 테스트 시나리오 추가

**2.1 [보안 테스트 유형]**
```javascript
async function testSecurity() {
  // 보안 테스트 코드
}
```

**2.2 [보안 테스트 유형]**
```javascript
async function testSecurity2() {
  // 보안 테스트 코드
}
```

---

#### 3. 성능 테스트 추가

```javascript
async function testPerformance() {
  const tests = [
    { name: '[테스트명]', url: '[URL]' },
  ];

  for (const test of tests) {
    const start = Date.now();
    await performTest(test.url);
    const duration = Date.now() - start;

    console.log(`${test.name}: ${duration}ms`);

    if (duration > 1000) {
      console.log(`⚠️  ${test.name} is slow (>${duration}ms)`);
    }
  }
}
```

---

#### 4. 에러 시나리오 테스트

**4.1 [에러 시나리오]**
```javascript
// 에러 처리 테스트 코드
```

**4.2 [에러 시나리오]**
```javascript
// 에러 처리 테스트 코드
```

---

### 📈 테스트 커버리지 분석

#### 현재 커버리지
| 영역 | 커버리지 | 비고 |
|------|---------|------|
| [영역1] | XX% | ✅/⚠️/❌ [상태 설명] |
| [영역2] | XX% | ✅/⚠️/❌ [상태 설명] |
| [영역3] | XX% | ✅/⚠️/❌ [상태 설명] |

#### 목표 커버리지 (개선 후)
| 영역 | 목표 | 우선순위 |
|------|------|---------|
| [영역1] | XX% | 높음/중간/낮음 |
| [영역2] | XX% | 높음/중간/낮음 |
| [영역3] | XX% | 높음/중간/낮음 |

---

### 🚀 실행 가이드

#### 기본 실행
```bash
cd c:/GitHub/[프로젝트명]
node scripts/[테스트스크립트명].cjs
```

#### [특정 작업]
```bash
[명령어]
```

#### [특정 작업]
```bash
[명령어]
```

---

### 📝 결론 및 권장사항

#### ✅ 현재 상태
- [검증 항목]: **[상태]**
- [검증 항목]: **[상태]**
- [배포 가능 여부]: **[예/아니오]**

#### ⚠️ 개선 필요 사항
1. **[개선 사항]** (우선순위: 높음/중간/낮음)
2. **[개선 사항]** (우선순위: 높음/중간/낮음)
3. **[개선 사항]** (우선순위: 높음/중간/낮음)

#### 🎯 다음 단계
1. [다음 작업]
2. [다음 작업]
3. [다음 작업]
4. [다음 작업]

---

**테스트 담당:** [담당자명]
**리뷰 필요:** ✅/❌
**배포 승인:** 승인/대기 중/거부
